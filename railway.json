{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build -w schemas && npm run build -w api"
  },
  "deploy": {
    "startCommand": "npm run start:prod -w api",
    "healthcheckPath": "/health"
  }
}
